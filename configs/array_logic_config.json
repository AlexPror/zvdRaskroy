{
  "array_patterns": {
    "распорка": {
      "description": "Распорки по длине (массив по направлению)",
      "part_name_pattern": "Распорка",
      "array_direction": "L",
      "quantity_rules": [
        {
          "condition": "L1 >= 500 and L1 < 1500",
          "quantity": 2,
          "description": "500-1499 мм: 2 распорки"
        },
        {
          "condition": "L1 >= 1500 and L1 < 2000",
          "quantity": 3,
          "description": "1500-1999 мм: 3 распорки"
        },
        {
          "condition": "L1 >= 2000 and L1 <= 2400",
          "quantity": 4,
          "description": "2000-2400 мм: 4 распорки"
        },
        {
          "condition": "L1 > 2400",
          "quantity": 5,
          "description": "Более 2400 мм: 5 распорок"
        }
      ],
      "spacing_formula": "L1 / (quantity + 1)",
      "notes": "Распорки располагаются равномерно по длине"
    }
  },
  
  "variable_for_quantity": {
    "name": "Количество_распорок",
    "type": "integer",
    "expression_kompas": "if(L1<1500; 2; if(L1<2000; 3; 4))",
    "description": "Количество распорок в зависимости от длины"
  },
  
  "implementation": {
    "method_1": {
      "name": "Переменная в КОМПАС-3D",
      "description": "Создать переменную 'Количество_распорок' с условным выражением",
      "expression": "if(L1<1500; 2; if(L1<2000; 3; 4))",
      "pros": ["Работает внутри КОМПАС", "Видна в дереве переменных"],
      "cons": ["Ограниченный синтаксис", "Сложно редактировать"]
    },
    "method_2": {
      "name": "Вычисление в приложении",
      "description": "Python вычисляет количество и обновляет массив",
      "pros": ["Гибкая логика", "Легко редактировать", "Можно добавлять/удалять экземпляры"],
      "cons": ["Требует API для работы с массивами"]
    }
  },
  
  "recommended_approach": {
    "step_1": "Создать переменную 'Количество_распорок' в сборке",
    "step_2": "Использовать эту переменную в массиве распорок",
    "step_3": "Приложение обновляет L1 → переменная пересчитывается → массив обновляется"
  }
}
